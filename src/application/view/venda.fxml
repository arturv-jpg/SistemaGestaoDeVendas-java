<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>
<?import javafx.scene.text.Font?>

<VBox xmlns="http://javafx.com/javafx/24"
      xmlns:fx="http://javafx.com/fxml/1"
      fx:controller="application.controller.VendaController"
      spacing="10"
      style="-fx-padding: 15; -fx-background-color: #ecf0f1;">

    <!-- 🔹 TÍTULO -->
    <Label text="Sistema de Vendas"
           maxWidth="Infinity"
           alignment="CENTER"
           style="-fx-background-color: #2c3e50; -fx-text-fill: white;">
        <font>
            <Font size="16"/>
        </font>
    </Label>

    <!-- 🔹 LINHA PRODUTO -->
    <HBox spacing="10">
        <VBox>
            <Label text="Produto"/>
            <TextField fx:id="txtProduto" prefWidth="200"/>
        </VBox>

        <VBox>
            <Label text="Quantidade"/>
            <TextField fx:id="txtQtd" prefWidth="80"/>
        </VBox>

        <Button text="Adicionar"
                prefWidth="120"
                style="-fx-background-color: #27ae60; -fx-text-fill: white;"
                onAction="#adicionarItem"/>
    </HBox>

    <!-- 🔹 CLIENTE -->
    <VBox>
        <Label text="Cliente"/>
        <ComboBox fx:id="cbCliente"
                  prefWidth="300"
                  promptText="Selecione o cliente"/>
    </VBox>

    <!-- 🔹 TABELA -->
    <TableView fx:id="tabItens" VBox.vgrow="ALWAYS">
        <columns>
            <TableColumn fx:id="colNome" text="Produto" prefWidth="250"/>
            <TableColumn fx:id="colQtd" text="Quantidade" prefWidth="120"/>
            <TableColumn fx:id="colPreco" text="Preço Unit." prefWidth="150"/>
        </columns>
    </TableView>

    <!-- 🔹 TOTAL -->
    <Label fx:id="lblTotal"
           text="Total: R$ 0.00"
           style="-fx-font-size: 14px; -fx-font-weight: bold;"/>

    <!-- 🔹 BOTÕES -->
    <HBox spacing="10">
        <Button text="Finalizar Venda"
                prefWidth="180"
                style="-fx-background-color: #2980b9; -fx-text-fill: white;"
                onAction="#finalizarVenda"/>

        <Button text="Cancelar Venda"
                prefWidth="180"
                style="-fx-background-color: #c0392b; -fx-text-fill: white;"
                onAction="#cancelarVenda"/>

        <Button text="Cancelar Última Venda"
                prefWidth="200"
                style="-fx-background-color: #e67e22; -fx-text-fill: white;"
                onAction="#cancelarUltimaVenda"/>
    </HBox>

</VBox>